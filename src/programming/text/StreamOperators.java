package programming.text;

public class StreamOperators {
    /*
        INTERMEDIATE OPERATIONS
            -> Operations that are performed on the stream and return the stream
            -> distinct(), map(), sorted(), filter()
     */
    /*
        TERMINAL OPERATIONS
            -> returns another type
            -> collect(), forEach(), reduce()
     */
}
